pre-commit:
    parallel: true
    jobs:
        - name: typos
          run: typos
        - name: lint & test
          glob: "*.go"
          group:
              jobs:
                  - glob: "*.go"
                    run: just lint-fix
                    stage_fixed: true
                  - glob: "*.go"
                    run: just test-all
